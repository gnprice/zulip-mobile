/* eslint-disable no-unused-expressions */
/* eslint-disable no-void */
/* eslint-disable func-names */
/* eslint-disable no-shadow */
/* eslint-disable no-underscore-dangle */
/* eslint-disable no-var */
/* eslint-disable no-use-before-define */
/* eslint-disable no-multi-assign */
/* eslint-disable no-return-assign */
/* eslint-disable prefer-rest-params */
/* eslint-disable no-sequences */
/* eslint-disable vars-on-top */ // !!
/* eslint-disabl block-scoped-var */ // !!
!(function () {
  function o(o) {
    const t = ['MSIE ', 'Trident/', 'Edge/'];
    return new RegExp(t.join('|')).test(o);
  }
  function t() {
    function t(o, t) {
      (this.scrollLeft = o), (this.scrollTop = t);
    }
    function r(o) {
      return 0.5 * (1 - Math.cos(Math.PI * o));
    }
    function i(o) {
      if (
        o === null
        || typeof o !== 'object'
        || void 0 === o.behavior
        || o.behavior === 'auto'
        || o.behavior === 'instant'
      ) {
        return !0;
      }
      if (typeof o === 'object' && o.behavior === 'smooth') {
        return !1;
      }
      throw new TypeError(
        `behavior member of ScrollOptions ${o.behavior} is not a valid value for enumeration ScrollBehavior.`,
      );
    }
    function s(o, t) {
      return t === 'Y'
        ? o.clientHeight + h < o.scrollHeight
        : t === 'X'
        ? o.clientWidth + h < o.scrollWidth
        : void 0;
    }
    function c(o, t) {
      const e = l.getComputedStyle(o, null)[`overflow${t}`];
      return e === 'auto' || e === 'scroll';
    }
    function n(o) {
      const t = s(o, 'Y') && c(o, 'Y');
      const l = s(o, 'X') && c(o, 'X');
      return t || l;
    }
    function f(o) {
      let t;
      do {
        t = (o = o.parentNode) === e.body;
      } while (!1 === t && !1 === n(o));
      return (t = null), o;
    }
    function a(o) {
      let t;
      let e;
      let i;
      let s = (y() - o.startTime) / v;
      (t = r((s = s > 1 ? 1 : s))),
        (e = o.startX + (o.x - o.startX) * t),
        (i = o.startY + (o.y - o.startY) * t),
        o.method.call(o.scrollable, e, i),
        (e === o.x && i === o.y) || l.requestAnimationFrame(a.bind(l, o));
    }
    function p(o, r, i) {
      let s;
      let c;
      let n;
      let f;
      const p = y();
      o === e.body
        ? ((s = l),
          (c = l.scrollX || l.pageXOffset),
          (n = l.scrollY || l.pageYOffset),
          (f = u.scroll))
        : ((s = o), (c = o.scrollLeft), (n = o.scrollTop), (f = t)),
        a({ scrollable: s, method: f, startTime: p, startX: c, startY: n, x: r, y: i });
    }
    if (!('scrollBehavior' in e.documentElement.style && !0 !== l.__forceSmoothScrollPolyfill__)) {
      const d = l.HTMLElement || l.Element;
      var v = 468;
      var h = o(l.navigator.userAgent) ? 1 : 0;
      var u = {
        scroll: l.scroll || l.scrollTo,
        scrollBy: l.scrollBy,
        elementScroll: d.prototype.scroll || t,
        scrollIntoView: d.prototype.scrollIntoView,
      };
      var y = l.performance && l.performance.now ? l.performance.now.bind(l.performance) : Date.now;
      (l.scroll = l.scrollTo = function () {
        void 0 !== arguments[0]
          && (!0 !== i(arguments[0])
            ? p.call(
                l,
                e.body,
                void 0 !== arguments[0].left ? ~~arguments[0].left : l.scrollX || l.pageXOffset,
                void 0 !== arguments[0].top ? ~~arguments[0].top : l.scrollY || l.pageYOffset,
              )
            : u.scroll.call(
                l,
                void 0 !== arguments[0].left
                  ? arguments[0].left
                  : typeof arguments[0] !== 'object'
                  ? arguments[0]
                  : l.scrollX || l.pageXOffset,
                void 0 !== arguments[0].top
                  ? arguments[0].top
                  : void 0 !== arguments[1]
                  ? arguments[1]
                  : l.scrollY || l.pageYOffset,
              ));
      }),
        (l.scrollBy = function () {
          void 0 !== arguments[0]
            && (i(arguments[0])
              ? u.scrollBy.call(
                  l,
                  void 0 !== arguments[0].left
                    ? arguments[0].left
                    : typeof arguments[0] !== 'object'
                    ? arguments[0]
                    : 0,
                  void 0 !== arguments[0].top
                    ? arguments[0].top
                    : void 0 !== arguments[1]
                    ? arguments[1]
                    : 0,
                )
              : p.call(
                  l,
                  e.body,
                  ~~arguments[0].left + (l.scrollX || l.pageXOffset),
                  ~~arguments[0].top + (l.scrollY || l.pageYOffset),
                ));
        }),
        (d.prototype.scroll = d.prototype.scrollTo = function () {
          if (void 0 !== arguments[0]) {
            if (!0 !== i(arguments[0])) {
              const o = arguments[0].left;
              const t = arguments[0].top;
              p.call(
                this,
                this,
                void 0 === o ? this.scrollLeft : ~~o,
                void 0 === t ? this.scrollTop : ~~t,
              );
            } else {
              if (typeof arguments[0] === 'number' && void 0 === arguments[1]) {
                throw new SyntaxError("Value couldn't be converted");
              }
              u.elementScroll.call(
                this,
                void 0 !== arguments[0].left
                  ? ~~arguments[0].left
                  : typeof arguments[0] !== 'object'
                  ? ~~arguments[0]
                  : this.scrollLeft,
                void 0 !== arguments[0].top
                  ? ~~arguments[0].top
                  : void 0 !== arguments[1]
                  ? ~~arguments[1]
                  : this.scrollTop,
              );
            }
          }
        }),
        (d.prototype.scrollBy = function () {
          void 0 !== arguments[0]
            && (!0 !== i(arguments[0])
              ? this.scroll({
                  left: ~~arguments[0].left + this.scrollLeft,
                  top: ~~arguments[0].top + this.scrollTop,
                  behavior: arguments[0].behavior,
                })
              : u.elementScroll.call(
                  this,
                  void 0 !== arguments[0].left
                    ? ~~arguments[0].left + this.scrollLeft
                    : ~~arguments[0] + this.scrollLeft,
                  void 0 !== arguments[0].top
                    ? ~~arguments[0].top + this.scrollTop
                    : ~~arguments[1] + this.scrollTop,
                ));
        }),
        (d.prototype.scrollIntoView = function () {
          if (!0 !== i(arguments[0])) {
            const o = f(this);
            const t = o.getBoundingClientRect();
            const r = this.getBoundingClientRect();
            o !== e.body
              ? (p.call(this, o, o.scrollLeft + r.left - t.left, o.scrollTop + r.top - t.top),
                l.getComputedStyle(o).position !== 'fixed'
                  && l.scrollBy({ left: t.left, top: t.top, behavior: 'smooth' }))
              : l.scrollBy({ left: r.left, top: r.top, behavior: 'smooth' });
          } else {
            u.scrollIntoView.call(this, void 0 === arguments[0] || arguments[0]);
          }
        });
    }
  }
  var l = window;
  var e = document;
  typeof exports === 'object' ? (module.exports = { polyfill: t }) : t();
}());
