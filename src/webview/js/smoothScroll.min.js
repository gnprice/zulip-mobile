// @flow strict-local
/* eslint-disable no-unused-expressions */
/* eslint-disable no-void */
/* eslint-disable func-names */
/* eslint-disable no-shadow */
/* eslint-disable no-underscore-dangle */
/* eslint-disable no-var */
/* eslint-disable no-use-before-define */
/* eslint-disable no-multi-assign */
/* eslint-disable no-return-assign */
/* eslint-disable prefer-rest-params */
/* eslint-disable no-sequences */
/* eslint-disable vars-on-top */ // !!
/* eslint-disable block-scoped-var */ // !!
!(function () {
  function getIsIEOrEdge(o) {
    const t = ['MSIE ', 'Trident/', 'Edge/'];
    return new RegExp(t.join('|')).test(o);
  }
  function polyfill() {
    function setScroll(o, t) {
      (this.scrollLeft = o), (this.scrollTop = t);
    }
    function easing(o) {
      return 0.5 * (1 - Math.cos(Math.PI * o));
    }
    function getIsNonsmooth(scrollOptions) {
      if (
        scrollOptions === null
        || typeof scrollOptions !== 'object'
        || void 0 === scrollOptions.behavior
        || scrollOptions.behavior === 'auto'
        || scrollOptions.behavior === 'instant'
      ) {
        return !0;
      }
      if (typeof scrollOptions === 'object' && scrollOptions.behavior === 'smooth') {
        return !1;
      }
      throw new TypeError(
        `behavior member of ScrollOptions ${scrollOptions.behavior} is not a valid value for enumeration ScrollBehavior.`,
      );
    }
    function s(o, t) {
      return t === 'Y'
        ? o.clientHeight + isIEOrEdge < o.scrollHeight
        : t === 'X'
        ? o.clientWidth + isIEOrEdge < o.scrollWidth
        : void 0;
    }
    function c(o, t) {
      const e = window_.getComputedStyle(o, null)[`overflow${t}`];
      return e === 'auto' || e === 'scroll';
    }
    function n(o) {
      const t = s(o, 'Y') && c(o, 'Y');
      const l = s(o, 'X') && c(o, 'X');
      return t || l;
    }
    function f(o) {
      let t;
      do {
        t = (o = o.parentNode) === document_.body;
      } while (!1 === t && !1 === n(o));
      return (t = null), o;
    }
    function smoothScrollImpl(o) {
      let fractionComplete;
      let curX;
      let curY;
      let timeFraction = (performanceNow() - o.startTime) / duration;
      (fractionComplete = easing((timeFraction = timeFraction > 1 ? 1 : timeFraction))),
        (curX = o.startX + (o.x - o.startX) * fractionComplete),
        (curY = o.startY + (o.y - o.startY) * fractionComplete),
        // This line attempts an "instant" scroll to where we want to be right now.
        o.method.call(o.scrollable, curX, curY),
        // And then this `requestAnimationFrame` is to let us repeat that until it's done.
        (curX === o.x && curY === o.y)
          || window_.requestAnimationFrame(smoothScrollImpl.bind(window_, o));
    }
    function smoothScroll(o, x, y) {
      let scrollable;
      let startX;
      let startY;
      let method;
      const startTime = performanceNow();
      o === document_.body
        ? ((scrollable = window_),
          (startX = window_.scrollX || window_.pageXOffset),
          (startY = window_.scrollY || window_.pageYOffset),
          (method = baseMethods.scroll))
        : ((scrollable = o), (startX = o.scrollLeft), (startY = o.scrollTop), (method = setScroll)),
        smoothScrollImpl({
          scrollable,
          method,
          startTime,
          startX,
          startY,
          x,
          y,
        });
    }
    if (
      !(
        'scrollBehavior' in document_.documentElement.style
        && !0 !== window_.__forceSmoothScrollPolyfill__
      )
    ) {
      const HTMLElement_ = window_.HTMLElement || window_.Element;
      var duration = 468;
      var isIEOrEdge = getIsIEOrEdge(window_.navigator.userAgent) ? 1 : 0;
      var baseMethods = {
        scroll: window_.scroll || window_.scrollTo,
        scrollBy: window_.scrollBy,
        elementScroll: HTMLElement_.prototype.scroll || setScroll,
        scrollIntoView: HTMLElement_.prototype.scrollIntoView,
      };
      var performanceNow =
        window_.performance && window_.performance.now
          ? window_.performance.now.bind(window_.performance)
          : Date.now;
      (window_.scroll = window_.scrollTo = function () {
        void 0 !== arguments[0]
          && (!0 !== getIsNonsmooth(arguments[0])
            ? smoothScroll.call(
                window_,
                document_.body,
                void 0 !== arguments[0].left
                  ? ~~arguments[0].left
                  : window_.scrollX || window_.pageXOffset,
                void 0 !== arguments[0].top
                  ? ~~arguments[0].top
                  : window_.scrollY || window_.pageYOffset,
              )
            : baseMethods.scroll.call(
                window_,
                void 0 !== arguments[0].left
                  ? arguments[0].left
                  : typeof arguments[0] !== 'object'
                  ? arguments[0]
                  : window_.scrollX || window_.pageXOffset,
                void 0 !== arguments[0].top
                  ? arguments[0].top
                  : void 0 !== arguments[1]
                  ? arguments[1]
                  : window_.scrollY || window_.pageYOffset,
              ));
      }),
        (window_.scrollBy = function () {
          void 0 !== arguments[0]
            && (getIsNonsmooth(arguments[0])
              ? baseMethods.scrollBy.call(
                  window_,
                  void 0 !== arguments[0].left
                    ? arguments[0].left
                    : typeof arguments[0] !== 'object'
                    ? arguments[0]
                    : 0,
                  void 0 !== arguments[0].top
                    ? arguments[0].top
                    : void 0 !== arguments[1]
                    ? arguments[1]
                    : 0,
                )
              : smoothScroll.call(
                  window_,
                  document_.body,
                  ~~arguments[0].left + (window_.scrollX || window_.pageXOffset),
                  ~~arguments[0].top + (window_.scrollY || window_.pageYOffset),
                ));
        }),
        (HTMLElement_.prototype.scroll = HTMLElement_.prototype.scrollTo = function () {
          if (void 0 !== arguments[0]) {
            if (!0 !== getIsNonsmooth(arguments[0])) {
              const o = arguments[0].left;
              const t = arguments[0].top;
              smoothScroll.call(
                this,
                this,
                void 0 === o ? this.scrollLeft : ~~o,
                void 0 === t ? this.scrollTop : ~~t,
              );
            } else {
              if (typeof arguments[0] === 'number' && void 0 === arguments[1]) {
                throw new SyntaxError("Value couldn't be converted");
              }
              baseMethods.elementScroll.call(
                this,
                void 0 !== arguments[0].left
                  ? ~~arguments[0].left
                  : typeof arguments[0] !== 'object'
                  ? ~~arguments[0]
                  : this.scrollLeft,
                void 0 !== arguments[0].top
                  ? ~~arguments[0].top
                  : void 0 !== arguments[1]
                  ? ~~arguments[1]
                  : this.scrollTop,
              );
            }
          }
        }),
        (HTMLElement_.prototype.scrollBy = function () {
          void 0 !== arguments[0]
            && (!0 !== getIsNonsmooth(arguments[0])
              ? this.scroll({
                  left: ~~arguments[0].left + this.scrollLeft,
                  top: ~~arguments[0].top + this.scrollTop,
                  behavior: arguments[0].behavior,
                })
              : baseMethods.elementScroll.call(
                  this,
                  void 0 !== arguments[0].left
                    ? ~~arguments[0].left + this.scrollLeft
                    : ~~arguments[0] + this.scrollLeft,
                  void 0 !== arguments[0].top
                    ? ~~arguments[0].top + this.scrollTop
                    : ~~arguments[1] + this.scrollTop,
                ));
        }),
        (HTMLElement_.prototype.scrollIntoView = function () {
          if (!0 !== getIsNonsmooth(arguments[0])) {
            const o = f(this);
            const t = o.getBoundingClientRect();
            const r = this.getBoundingClientRect();
            o !== document_.body
              ? (smoothScroll.call(
                  this,
                  o,
                  o.scrollLeft + r.left - t.left,
                  o.scrollTop + r.top - t.top,
                ),
                window_.getComputedStyle(o).position !== 'fixed'
                  && window_.scrollBy({ left: t.left, top: t.top, behavior: 'smooth' }))
              : window_.scrollBy({ left: r.left, top: r.top, behavior: 'smooth' });
          } else {
            baseMethods.scrollIntoView.call(this, void 0 === arguments[0] || arguments[0]);
          }
        });
    }
  }
  var window_ = window;
  var document_ = document;
  typeof exports === 'object' ? (module.exports = { polyfill }) : polyfill();
}());
