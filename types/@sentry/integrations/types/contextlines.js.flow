/* @flow
 * @generated by TsFlower
 */
import {
    type Event,
    type EventProcessor,
    type Hub,
    type Integration,
    type StackFrame,
} from "@sentry/types";

declare interface ContextLinesOptions { frameContextLines?: number }

declare export class ContextLines implements Integration {
    +_options: any,
    id: string,
    name: string,
    constructor(_options?: ContextLinesOptions): any,
    setupOnce(
        addGlobalEventProcessor: (callback: EventProcessor) => void,
        getCurrentHub: () => Hub
    ): void,
    addSourceContext(event: Event): Event,
};

declare export function applySourceContextToFrame(
    frame: StackFrame,
    htmlLines: string[],
    htmlFilename: string,
    linesOfContext: number
): StackFrame;

export {};
