/* @flow
 * @generated by TsFlower
 */
import {
    type Event,
    type EventProcessor,
    type Hub,
    type Integration,
    type StackFrame,
} from "@sentry/types";

type StackFrameIteratee = (frame: StackFrame) => StackFrame;

declare export class RewriteFrames implements Integration {
    id: string,
    name: string,
    +_root?: any,
    +_prefix: any,
    constructor(
        options?: {
            root?: string,
            prefix?: string,
            iteratee?: StackFrameIteratee,
            ...
        }
    ): any,
    setupOnce(
        addGlobalEventProcessor: (callback: EventProcessor) => void,
        getCurrentHub: () => Hub
    ): void,
    process(originalEvent: Event): Event,
    +_iteratee: any,
    _processExceptionsEvent: any,
    _processStacktrace: any,
};

export {};
