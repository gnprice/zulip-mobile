/* @flow
 * @generated by TsFlower
 */
import { type Event, type EventProcessor, type Hub, type Integration } from "@sentry/types";

type LocalForage = {
    setItem<T>(key: string, value: T, callback?: (err: any, value: T) => void): Promise<T>,
    iterate<T, U>(
        iteratee: (value: T, key: string, iterationNumber: number) => U,
        callback?: (err: any, result: U) => void
    ): Promise<U>,
    removeItem(key: string, callback?: (err: any) => void): Promise<void>,
    length(): Promise<number>,
    ...
};

export type Item = {
    key: string,
    value: Event,
    ...
};

declare export class Offline implements Integration {
    id: string,
    +name: string,
    hub?: Hub,
    maxStoredEvents: number,
    offlineEventStore: LocalForage,
    constructor(options?: { maxStoredEvents?: number, ... }): any,
    setupOnce(
        addGlobalEventProcessor: (callback: EventProcessor) => void,
        getCurrentHub: () => Hub
    ): void,
    _cacheEvent: any,
    _enforceMaxEvents: any,
    _purgeEvent: any,
    _purgeEvents: any,
    _sendEvents: any,
};

export {};
