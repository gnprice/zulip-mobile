/* @flow
 * @generated by TsFlower
 */
import {
    type Instrumenter,
    type Primitive,
    type Span as SpanInterface,
    type SpanContext,
    type SpanOrigin,
    type TraceContext,
    type Transaction,
} from "@sentry/types";

declare export class SpanRecorder {
    spans: Span[],
    +_maxlen: any,
    constructor(maxlen?: number): any,
    add(span: Span): void,
};

declare export class Span implements SpanInterface {
    traceId: string,
    spanId: string,
    parentSpanId?: string,
    status?: SpanStatusType | string,
    sampled?: boolean,
    startTimestamp: number,
    endTimestamp?: number,
    op?: string,
    description?: string,
    tags: { [key: string]: Primitive, ... },
    data: { [key: string]: any, ... },
    spanRecorder?: SpanRecorder,
    transaction?: Transaction,
    instrumenter: Instrumenter,
    origin?: SpanOrigin,
    constructor(spanContext?: SpanContext): any,
    get name(): string,
    set name(name: string): void,
    startChild(
        spanContext?: Pick<SpanContext, Exclude<$Keys<SpanContext>, "sampled" | "traceId" | "parentSpanId">>
    ): Span,
    setTag(key: string, value: Primitive): this,
    setData(key: string, value: any): this,
    setStatus(value: SpanStatusType): this,
    setHttpStatus(httpStatus: number): this,
    setName(name: string): void,
    isSuccess(): boolean,
    finish(endTimestamp?: number): void,
    toTraceparent(): string,
    toContext(): SpanContext,
    updateWithContext(spanContext: SpanContext): this,
    getTraceContext(): TraceContext,
    toJSON(): {
        data?: { [key: string]: any, ... },
        description?: string,
        op?: string,
        parent_span_id?: string,
        span_id: string,
        start_timestamp: number,
        status?: string,
        tags?: { [key: string]: Primitive, ... },
        timestamp?: number,
        trace_id: string,
        origin?: SpanOrigin,
        ...
    },
};

export type SpanStatusType = "ok" | "deadline_exceeded" | "unauthenticated" | "permission_denied" | "not_found" | "resource_exhausted" | "invalid_argument" | "unimplemented" | "unavailable" | "internal_error" | "unknown_error" | "cancelled" | "already_exists" | "failed_precondition" | "aborted" | "out_of_range" | "data_loss";
declare export function spanStatusfromHttpCode(httpStatus: number): SpanStatusType;
