/* @flow
 * @generated by TsFlower
 */
import {
    type BaseTransportOptions,
    type Envelope,
    type EnvelopeItemType,
    type Event,
    type Transport,
} from "@sentry/types";

declare interface MatchParam {
    envelope: Envelope,
    getEvent(types?: EnvelopeItemType[]): Event | void,
}

type RouteTo = {
    dsn: string,
    release: string,
    ...
};

type Matcher = (param: MatchParam) => string | RouteTo[];
declare export function eventFromEnvelope(env: Envelope, types: EnvelopeItemType[]): Event | void;
declare export function makeMultiplexedTransport<TO: BaseTransportOptions>(createTransport: (options: TO) => Transport, matcher: Matcher): (options: TO) => Transport;
export {};
