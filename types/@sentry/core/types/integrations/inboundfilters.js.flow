/* @flow
 * @generated by TsFlower
 */
import type { Partial } from "tsflower/subst/lib";
import { type Event, type EventProcessor, type Hub, type Integration } from "@sentry/types";

export interface InboundFiltersOptions {
    allowUrls: Array<string | RegExp>,
    denyUrls: Array<string | RegExp>,
    ignoreErrors: Array<string | RegExp>,
    ignoreTransactions: Array<string | RegExp>,
    ignoreInternal: boolean,
    disableErrorDefaults: boolean,
    disableTransactionDefaults: boolean,
};

declare export class InboundFilters implements Integration {
    id: string,
    name: string,
    +_options: any,
    constructor(options?: Partial<InboundFiltersOptions>): any,
    setupOnce(
        addGlobalEventProcessor: (processor: EventProcessor) => void,
        getCurrentHub: () => Hub
    ): void,
};

declare export function _mergeOptions(
    internalOptions?: Partial<InboundFiltersOptions>,
    clientOptions?: Partial<InboundFiltersOptions>
): Partial<InboundFiltersOptions>;

declare export function _shouldDropEvent(event: Event, options: Partial<InboundFiltersOptions>): boolean;
