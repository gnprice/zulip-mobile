/* @flow
 * @generated by TsFlower
 */
import { type EventProcessor, type Hub, type Integration } from '@sentry/types';

declare export class DeviceContext implements Integration {
  id: string;
  name: string;
  setupOnce(
    addGlobalEventProcessor: (callback: EventProcessor) => void,
    getCurrentHub: () => Hub,
  ): void;
}
