/* @flow
 * @generated by TsFlower
 */
import type { PromiseLike } from 'tsflower/subst/lib';
import { type BaseTransportOptions, type Envelope, type Transport } from '@sentry/types';
import { type PromiseBuffer } from '@sentry/utils';
declare export var DEFAULT_BUFFER_SIZE: 30;
export type BaseNativeTransport = BaseTransportOptions;
export interface BaseNativeTransportOptions {
  bufferSize?: number;
}

declare export class NativeTransport implements Transport {
  +_buffer: PromiseBuffer<void>;
  constructor(options?: BaseNativeTransportOptions): any;
  send(envelope: Envelope): PromiseLike<void>;
  flush(timeout?: number): PromiseLike<boolean>;
}

declare export function makeNativeTransport(options?: BaseNativeTransportOptions): NativeTransport;
declare export function makeNativeTransportFactory({ enableNative?: boolean, ... }):
  | typeof makeNativeTransport
  | null;
