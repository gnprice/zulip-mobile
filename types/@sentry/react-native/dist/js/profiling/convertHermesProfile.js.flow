/* @flow
 * @generated by TsFlower
 */
import { type ThreadCpuSample, type ThreadId } from '@sentry/types';
import * as Hermes from './hermes';
import { type RawThreadCpuProfile } from './types';
declare export function convertToSentryProfile(
  hermesProfile: Hermes.Profile,
): RawThreadCpuProfile | null;

declare export function mapSamples(
  hermesSamples: Hermes.Sample[],
  maxElapsedSinceStartNs?: number,
): {
  samples: ThreadCpuSample[],
  hermesStacks: Set<Hermes.StackFrameId>,
  jsThreads: Set<ThreadId>,
  ...
};
