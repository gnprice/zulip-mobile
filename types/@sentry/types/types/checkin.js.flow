/* @flow
 * @generated by TsFlower
 */
import { type TraceContext } from "./context";

declare interface CrontabSchedule {
    type: "crontab",
    value: string,
}

declare interface IntervalSchedule {
    type: "interval",
    value: number,
    unit: "year" | "month" | "week" | "day" | "hour" | "minute",
}

type MonitorSchedule = CrontabSchedule | IntervalSchedule;

export interface SerializedCheckIn {
    check_in_id: string,
    monitor_slug: string,
    status: "in_progress" | "ok" | "error",
    duration?: number,
    release?: string,
    environment?: string,
    monitor_config?: {
        schedule: MonitorSchedule,
        checkin_margin?: number,
        max_runtime?: number,
        timezone?: string,
        ...
    },
    contexts?: { trace?: TraceContext, ... },
};

declare interface InProgressCheckIn {
    monitorSlug: $ElementType<SerializedCheckIn, "monitor_slug">,
    status: "in_progress",
}

export interface FinishedCheckIn {
    monitorSlug: $ElementType<SerializedCheckIn, "monitor_slug">,
    status: "ok" | "error",
    checkInId: $ElementType<SerializedCheckIn, "check_in_id">,
    duration?: $ElementType<SerializedCheckIn, "duration">,
};

export type CheckIn = InProgressCheckIn | FinishedCheckIn;
type SerializedMonitorConfig = NonNullable<$ElementType<SerializedCheckIn, "monitor_config">>;

export interface MonitorConfig {
    schedule: MonitorSchedule,
    checkinMargin?: $ElementType<SerializedMonitorConfig, "checkin_margin">,
    maxRuntime?: $ElementType<SerializedMonitorConfig, "max_runtime">,
    timezone?: $ElementType<SerializedMonitorConfig, "timezone">,
};

export {};
