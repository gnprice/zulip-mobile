/* @flow
 * @generated by TsFlower
 */
import {
    type Attachment,
    type AttachmentItem,
    type DataCategory,
    type DsnComponents,
    type Envelope,
    type EnvelopeItemType,
    type Event,
    type EventEnvelopeHeaders,
    type SdkInfo,
    type SdkMetadata,
    type TextEncoderInternal,
} from "@sentry/types";

declare export function createEnvelope<E: Envelope>(headers: $ElementType<E, 0>, items?: $ElementType<E, 1>): E;
declare export function addItemToEnvelope<E: Envelope>(envelope: E, newItem: $ElementType<$ElementType<E, 1>, number>): E;

declare export function forEachEnvelopeItem<E: Envelope>(
    envelope: Envelope,
    callback: (
        envelopeItem: $ElementType<$ElementType<E, 1>, number>,
        envelopeItemType: $ElementType<$ElementType<$ElementType<$ElementType<E, 1>, number>, 0>, "type">
    ) => boolean | void
): boolean;

declare export function envelopeContainsItemType(envelope: Envelope, types: EnvelopeItemType[]): boolean;
declare export function serializeEnvelope(envelope: Envelope, textEncoder?: TextEncoderInternal): string | Uint8Array;
export interface TextDecoderInternal { decode(input?: Uint8Array): string };

declare export function parseEnvelope(
    env: string | Uint8Array,
    textEncoder: TextEncoderInternal,
    textDecoder: TextDecoderInternal
): Envelope;

declare export function createAttachmentEnvelopeItem(attachment: Attachment, textEncoder?: TextEncoderInternal): AttachmentItem;
declare export function envelopeItemTypeToDataCategory(type: EnvelopeItemType): DataCategory;
declare export function getSdkMetadataForEnvelopeHeader(metadataOrEvent?: SdkMetadata | Event): SdkInfo | void;

declare export function createEventEnvelopeHeaders(
    event: Event,
    sdkInfo: SdkInfo | void,
    tunnel: string | void,
    dsn: DsnComponents
): EventEnvelopeHeaders;
