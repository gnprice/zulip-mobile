/**
 * Flowtype definitions for createStackNavigator
 * Generated by Flowgen from a Typescript Definition
 * Flowgen v1.17.0
 * @flow
 */

import {
  DefaultNavigatorOptions,
  type StackRouterOptions,
  type StackNavigationState,
} from "@react-navigation/native";
import type {
  StackNavigationConfig,
  StackNavigationOptions,
  StackNavigationEventMap,
} from "../types";
declare type Props = {
  ...DefaultNavigatorOptions<StackNavigationOptions>,
  ...StackRouterOptions,
  ...StackNavigationConfig,
};
declare function StackNavigator(x: Props): React$Node;
// TODO ad-hoc fix $Exports
import * as $rnav$native from '@react-navigation/native';
declare var _default: <
  // TODO(flowgen-fix): somehow automate this covariance -- both of them
  ParamList: { +[key: string]: { +[key: string]: any } | void, ... }
>() =>
 // TODO(flowgen): Oh yikes -- these had type arguments.  They got dropped!
 //   Dropped perhaps related to the $Exports thing?  That means it probably
 //   requires a fix in flowgen itself...
 $rnav$native.TypedNavigator;
declare export default typeof _default;
