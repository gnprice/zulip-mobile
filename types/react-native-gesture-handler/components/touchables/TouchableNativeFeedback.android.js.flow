/**
 * Flowtype definitions for TouchableNativeFeedback.android
 * Generated by Flowgen from a Typescript Definition
 * Flowgen v1.17.0
 * @flow
 */

import { TouchableNativeFeedbackProps, ColorValue } from "react-native";
import { Component } from "react";
import { GenericTouchableProps } from "./GenericTouchable";
export type TouchableNativeFeedbackExtraProps = {
  borderless?: boolean,
  rippleColor?: number | null,
  rippleRadius?: number | null,
  foreground?: boolean,
  ...
};
/**
 * TouchableNativeFeedback behaves slightly different than RN's TouchableNativeFeedback.
 * There's small difference with handling long press ripple since RN's implementation calls
 * ripple animation via bridge. This solution leaves all animations' handling for native components so
 * it follows native behaviours.
 */
declare export default class TouchableNativeFeedback
  mixins
    Component<{ ...TouchableNativeFeedbackProps, ...GenericTouchableProps }>
{
  static defaultProps: {
    useForeground: boolean,
    extraButtonProps: {
      rippleColor: null,
      ...
    },
    delayLongPress: number,
    ...
  };
  static SelectableBackground: (rippleRadius?: number | void) => {
    type: string,
    attribute: string,
    rippleRadius: number | void,
    ...
  };
  static SelectableBackgroundBorderless: (rippleRadius?: number | void) => {
    type: string,
    attribute: string,
    rippleRadius: number | void,
    ...
  };
  static Ripple: (
    color: ColorValue,
    borderless: boolean,
    rippleRadius?: number | void
  ) => {
    type: string,
    color: string | Symbol,
    borderless: boolean,
    rippleRadius: number | void,
    ...
  };
  static canUseNativeForeground: () => boolean;
  getExtraButtonProps(): TouchableNativeFeedbackExtraProps;
  render(): React$Node;
}
