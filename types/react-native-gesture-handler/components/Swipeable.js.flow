/**
 * Flowtype definitions for Swipeable
 * Generated by Flowgen from a Typescript Definition
 * Flowgen v1.17.0
 * @flow
 */

import * as React from "react";
import { Component } from "react";
import { Animated } from "react-native";
import { type ViewStyle } from "react-native/Libraries/StyleSheet/StyleSheet";
import { PanGestureHandlerProps } from "../handlers/gestureHandlers";
declare type SwipeableExcludes = Exclude<
  $Keys<PanGestureHandlerProps>,
  "onGestureEvent" | "onHandlerStateChange"
>;
declare type SwipeableProps = {
  enableTrackpadTwoFingerGesture?: boolean,
  friction?: number,
  leftThreshold?: number,
  rightThreshold?: number,
  overshootLeft?: boolean,
  overshootRight?: boolean,
  overshootFriction?: number,
  onSwipeableLeftOpen?: () => void,
  onSwipeableRightOpen?: () => void,
  onSwipeableOpen?: () => void,
  onSwipeableClose?: () => void,
  onSwipeableLeftWillOpen?: () => void,
  onSwipeableRightWillOpen?: () => void,
  onSwipeableWillOpen?: () => void,
  onSwipeableWillClose?: () => void,

  /**
   *
   * This map describes the values to use as inputRange for extra interpolation:
   * AnimatedValue: [startValue, endValue]
   *
   * progressAnimatedValue: [0, 1]
   * dragAnimatedValue: [0, +]
   *
   * To support `rtl` flexbox layouts use `flexDirection` styling.
   */
  renderLeftActions?: (
    progressAnimatedValue: Animated.Interpolation,
    dragAnimatedValue: Animated.Interpolation
  ) => React.Node,

  /**
   *
   * This map describes the values to use as inputRange for extra interpolation:
   * AnimatedValue: [startValue, endValue]
   *
   * progressAnimatedValue: [0, 1]
   * dragAnimatedValue: [0, -]
   *
   * To support `rtl` flexbox layouts use `flexDirection` styling.
   */
  renderRightActions?: (
    progressAnimatedValue: Animated.Interpolation,
    dragAnimatedValue: Animated.Interpolation
  ) => React.Node,
  useNativeAnimations?: boolean,
  animationOptions?: { [key: string]: mixed, ... },
  containerStyle?: $ReactNative$GenericStyleProp<ViewStyle>,
  childrenContainerStyle?: $ReactNative$GenericStyleProp<ViewStyle>,
  ...
} & Pick;
declare type SwipeableState = {
  dragX: Animated.Value,
  rowTranslation: Animated.Value,
  rowState: number,
  leftWidth?: number,
  rightOffset?: number,
  rowWidth?: number,
  ...
};
declare export default class Swipeable
  mixins Component<SwipeableProps, SwipeableState>
{
  static defaultProps: {
    friction: number,
    overshootFriction: number,
    useNativeAnimations: boolean,
    ...
  };
  constructor(props: SwipeableProps): this;
  UNSAFE_componentWillUpdate(
    props: SwipeableProps,
    state: SwipeableState
  ): void;
  close: () => void;
  openLeft: () => void;
  openRight: () => void;
  render(): React$Node;
}
declare export {};
type $ReactNative$GenericStyleProp<+T> = null | void | T | false | "" | $ReadOnlyArray<$ReactNative$GenericStyleProp<T>>;
