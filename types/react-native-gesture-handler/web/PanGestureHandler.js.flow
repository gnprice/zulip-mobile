/**
 * Flowtype definitions for PanGestureHandler
 * Generated by Flowgen from a Typescript Definition
 * Flowgen v1.17.0
 * @flow
 */

import { EventMap } from "./constants";
import DraggingGestureHandler from "./DraggingGestureHandler";
import { State } from "../State";
import { Config, HammerInputExt } from "./GestureHandler";
declare class PanGestureHandler mixins DraggingGestureHandler {
  name: string;
  NativeGestureClass: PanRecognizerStatic;
  getHammerConfig(): {
    direction: number,
    pointers: number | void,
    ...
  };
  getState(type: $Keys<typeof EventMap>): State;
  getDirection(): number;
  getConfig(): $Rest<
    {
      enabled: boolean,
      minPointers: number,
      maxPointers: number,
      minDist: number,
      minDistSq: number,
      minVelocity: number,
      minVelocitySq: number,
      maxDist: number,
      maxDistSq: number,
      failOffsetXStart: number,
      failOffsetYStart: number,
      failOffsetXEnd: number,
      failOffsetYEnd: number,
      activeOffsetXStart: number,
      activeOffsetXEnd: number,
      activeOffsetYStart: number,
      activeOffsetYEnd: number,
      waitFor: any[] | null,
      ...
    },
    { ... }
  >;
  shouldFailUnderCustomCriteria(x: HammerInputExt, criteria: any): boolean;
  shouldActivateUnderCustomCriteria(x: any, criteria: any): boolean;
  shouldMultiFingerPanFail(x: {
    deltaRotation: number,
    pointerLength: number,
    scale: number,
    ...
  }): boolean;
  updateHasCustomActivationCriteria(criteria: {
    ...Config,
    ...{
      minVelocityX?: number,
      minVelocityY?: number,
      ...
    },
  }): boolean;
  isGestureEnabledForEvent(
    props: any,
    _recognizer: any,
    inputData: {
      ...HammerInputExt,
      ...{
        deltaRotation: number,
        ...
      },
    }
  ):
    | {
        failed: boolean,
        success?: void,
        ...
      }
    | {
        success: boolean,
        failed?: void,
        ...
      };
}
declare export default typeof PanGestureHandler;
