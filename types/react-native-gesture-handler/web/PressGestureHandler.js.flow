/**
 * Flowtype definitions for PressGestureHandler
 * Generated by Flowgen from a Typescript Definition
 * Flowgen v1.17.0
 * @flow
 */

import { State } from "../State";
import { HammerInputNames } from "./constants";
import DiscreteGestureHandler from "./DiscreteGestureHandler";
import { Config, HammerInputExt } from "./GestureHandler";
declare class PressGestureHandler mixins DiscreteGestureHandler {
  name: string;
  minDurationMs: any;
  maxDist: number | void;
  NativeGestureClass: PressRecognizerStatic;
  shouldDelayTouches: boolean;
  simulateCancelEvent(inputData: HammerInputExt): void;
  updateHasCustomActivationCriteria(x: {
    ...Config,
    ...{
      shouldCancelWhenOutside: boolean,
      ...
    },
  }): boolean;
  getState(type: $Keys<typeof HammerInputNames>): State;
  getConfig():
    | $Rest<
        {
          enabled: boolean,
          minPointers: number,
          maxPointers: number,
          minDist: number,
          minDistSq: number,
          minVelocity: number,
          minVelocitySq: number,
          maxDist: number,
          maxDistSq: number,
          failOffsetXStart: number,
          failOffsetYStart: number,
          failOffsetXEnd: number,
          failOffsetYEnd: number,
          activeOffsetXStart: number,
          activeOffsetXEnd: number,
          activeOffsetYStart: number,
          activeOffsetYEnd: number,
          waitFor: any[] | null,
          ...
        },
        { ... }
      >
    | {
        shouldCancelWhenOutside: boolean,
        maxDistSq: number,
        ...
      };
  getHammerConfig(): {
    time: any,
    pointers: number | void,
    ...
  };
  onGestureActivated(ev: HammerInputExt): void;
  shouldDelayTouchForEvent(x: HammerInputExt): boolean;
  onGestureStart(ev: HammerInputExt): void;
  sendGestureStartedEvent(ev: HammerInputExt): void;
  forceInvalidate(event: HammerInputExt): void;
  onRawEvent(ev: HammerInputExt): void;
  updateGestureConfig(x: {
    [x: string]: any,
    shouldActivateOnStart?: boolean | void,
    disallowInterruption?: boolean | void,
    shouldCancelWhenOutside?: boolean | void,
    minDurationMs?: number | void,
    maxDist?: number | void,
    minPointers?: number | void,
    maxPointers?: number | void,
    ...
  }): $Rest<
    {
      enabled: boolean,
      minPointers: number,
      maxPointers: number,
      minDist: number,
      minDistSq: number,
      minVelocity: number,
      minVelocitySq: number,
      maxDist: number,
      maxDistSq: number,
      failOffsetXStart: number,
      failOffsetYStart: number,
      failOffsetXEnd: number,
      failOffsetYEnd: number,
      activeOffsetXStart: number,
      activeOffsetXEnd: number,
      activeOffsetYStart: number,
      activeOffsetYEnd: number,
      waitFor: any[] | null,
      ...
    },
    { ... }
  >;
}
declare export default typeof PressGestureHandler;
